# 复制为 .env.development 后填写你电脑的局域网 IP（真机预览必填）
# 查 IP：Windows 在终端运行 ipconfig，找 IPv4（如 192.168.1.10）
# 后端需监听所有网卡：uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
VITE_API_BASE_URL=http://192.168.1.10:8000
